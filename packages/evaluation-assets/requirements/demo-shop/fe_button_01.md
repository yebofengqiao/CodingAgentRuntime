# FE_BUTTON_01

Update the demo-shop button lab page to use the shared `Button` component instead of a native HTML `<button>`.

Requirements:

- Edit `apps/demo-shop/src/pages/button-lab/index.tsx`.
- Import `Button` from `packages/ui/Button`.
- Render `<Button />` in place of the raw `<button>`.
- Do not modify `packages/ui/**`.
- Record the final run result in `.evaluation/mock-result.json`.
