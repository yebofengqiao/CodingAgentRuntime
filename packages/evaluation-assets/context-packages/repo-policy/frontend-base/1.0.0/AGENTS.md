# frontend-base repo policy

- Prefer feature-first edits under `apps/demo-shop/src/pages/**`.
- Reuse existing tracking and page-local helpers before creating new shared modules.
- Do not modify `packages/ui/**` in these evaluation tasks.
