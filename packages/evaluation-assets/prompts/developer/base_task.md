Execute the assigned coding task inside the prepared workspace.

Priorities:

1. Understand the task requirement and scope before editing.
2. Reuse the closest existing project patterns, components, and helpers.
3. Use the loaded repo policy and skill/package guidance when it applies.
4. Validate the result against the configured checks before finishing.

Do not stop after making code changes if validation has not been completed.
