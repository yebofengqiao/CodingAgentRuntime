# Repos

`cases/*.yaml` expect local git repos under this directory.

The bundled `demo-shop` repo is the only checked-in repo required for the public showcase.

If you need to rebuild it, use local git tooling inside this directory and keep the checked-in file tree as the source of truth.
